<h2>At this moment, the parking spot is: </h2>

<div>
  <%= button_to "Book Parking Spot", "#"%>
</div>

<p>
Interested in the booking history?
</p>

<div id="bookings">
  <% @bookings.each do |booking| %>
    <%= render booking %>
    <p>
      <%= link_to "Show this booking", booking %>
    </p>
  <% end %>
</div>

<%= link_to "New booking", new_booking_path %>

<div>
  <%= button_to "Sign Out", destroy_user_session_url, method: :delete %>
</div>
