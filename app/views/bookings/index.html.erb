<h2>
  At this moment, the parking spot is: 
  <% if ParkingSpot.first.available? %>
    <%= " Available" %>
  <% else %>
    <%= " Booked" %>
  <% end %>
</h2>

<div>
  <%= button_to "Book Parking Spot", bookings_book_url %>
  <%= button_to "Release Parking Spot", bookings_release_url %>
</div>

<strong>Booking History</strong>

<div id="bookings">
  <% @bookings.each do |booking| %>
    <%= render booking %>
  <% end %>
</div>

<div>
  <%= button_to "Sign Out", destroy_user_session_url, method: :delete %>
</div>
